@model Hotel.MVC.DTO.BookingDTO

@{
    ViewData["Title"] = "Create Booking";
}

<h2>Create Booking</h2>

<form asp-action="CreateBooking" method="post">
    @* <div class="form-group">
        <label asp-for="CustomerID">Customer ID</label>
        <input asp-for="CustomerID" class="form-control" />
        <span asp-validation-for="CustomerID" class="text-danger"></span>
    </div>
 *@
    <div class="form-group">
        <label asp-for="CheckInDate">Check-in Date</label>
        <input asp-for="CheckInDate" class="form-control" type="date" />
        <span asp-validation-for="CheckInDate" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="CheckOutDate">Check-out Date</label>
        <input asp-for="CheckOutDate" class="form-control" type="date" />
        <span asp-validation-for="CheckOutDate" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="NoOfPersons">Number of Persons</label>
        <input asp-for="NoOfPersons" class="form-control" type="number" />
        <span asp-validation-for="NoOfPersons" class="text-danger"></span>
    </div>

    @* Add additional fields as necessary based on your BookingDTO *@

    <button type="submit" class="btn btn-primary">Create Booking</button>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
