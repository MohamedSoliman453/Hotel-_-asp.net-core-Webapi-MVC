@model Hotel.MVC.DTO.BookingDTO

@{
    ViewData["Title"] = "Booking Details";
}

<h2>Booking Details</h2>

<div class="booking-details">
    <p><strong>Booking ID:</strong> @Model.ID</p>
    <p><strong>Customer ID:</strong> @Model.CustomerID</p>
    <p><strong>Check-In Date:</strong> @Model.CheckInDate.ToString("dd MMMM yyyy")</p>
    <p><strong>Check-Out Date:</strong> @Model.CheckOutDate.ToString("dd MMMM yyyy")</p>
    <p><strong>Number of Persons:</strong> @Model.NoOfPersons</p>
@* 
    @if (Model.RoomBookings != null && Model.RoomBookings.Any())
    {
        <h3>Rooms Booked</h3>
        <ul>
            @foreach (var roomBooking in Model.RoomBookings)
            {
                <li>Room ID: @roomBooking.RoomID - @roomBooking.RoomType (Status: @roomBooking.Status)</li>
            }
        </ul>
    } *@

    @* Add additional details as necessary *@
</div>
